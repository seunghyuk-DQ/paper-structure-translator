[project]
name = "structure-preserving-paper-translator"
version = "0.1.0"
description = "Chunked literal Korean translation pipeline for structure-preserving academic paper HTML."
requires-python = ">=3.11"
dependencies = [
  "beautifulsoup4>=4.14.3",
  "lxml>=6.1.0",
  "requests>=2.33.1",
]

[tool.uv]
package = false

